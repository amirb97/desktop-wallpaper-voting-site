<section>
    <div>
        <h1>{{wallpapers.1.title}}</h1>
        <img class="wallpaper" src="{{wallpapers.1.wallpaper_url}}" alt="{{wallpapers.1.title}}">
    </div>
    <div>
        <h1>{{wallpapers.2.title}}</h1>
        <img class="wallpaper" src="{{wallpapers.2.wallpaper_url}}" alt="{{wallpapers.2.title}}">
    </div>
</section>