<!DOCTYPE html>
<html lang="en">

    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Desktop Wallpaper Voting site</title>
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
        <link rel="stylesheet" href="/css/style.css">
    </head>


    <body>
        <div class="d-flex flex-column min-vh-100">
            <header class="col-12 p-3 bg-dark text-white justify-content-spacebetween">

                <div class="d-flex flex-wrap align-item-center justify-content-center justify-content-lg-start">
                    <a class="voto d-flex align-items-center text-decoration-none px-2 text-white normspro" href="/">Voto</a>
                </div>

                <div class="col-8 nav d-flex justify-content-around p-3">
                        <a class="text-light textstyle"  href="/">Home</a>
                        <a class="text-light textstyle" href="/profile">Profile</a>
                        <a class="text-light textstyle" href="/leaderboard">Leaderboard</a>
                        <a class="text-light textstyle" href="/vote">Vote</a>
                </div>

                <div class="text-end">
                    {{#if loggedIn}}
                    <button id="logout" class="btn btn-outline-light me-2">logout</button>
                    {{else}}
                    <a href="/login" class="btn btn-warning">login</a>
                    {{/if}}
                </div>
            </header>

            <main class="p-0 container">
                <div class="row">
                    <section class="col-12">
                        {{{ body }}}
                    </section>
                </div>
            </main>

            <footer class="p-3 bg-dark text-white text-center">
            Thanks for visiting!
            </footer>
        </div>
        {{#if loggedIn}}
        <script src="/js/logout.js"></script>
        {{/if}}
    </body>

</html>