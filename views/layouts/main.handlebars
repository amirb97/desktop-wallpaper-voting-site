<!DOCTYPE html>
<html lang="en">

    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Desktop Wallpaper Voting site</title>
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
        <link rel="stylesheet" href="/css/style.css">
    </head>


    <body>
        <div>

            <header class="p-3 bg-dark text-white justify-content-spacebetween">
                <div class="d-flex flex-wrap align-item-center justify-content-center justify-content-lg-start">
                    <a class="d-flex align-items-center text-decoration-none px-2 text-white" href="/">Desktop Wallpaper Voting site</a>
                </div>
                        <div class="d-flex alignt-item-center text-end">
                            {{#if loggedIn}}
                            <button id="logout" class="btn btn-outline-light me-2">logout</button>
                            {{else}}
                            <a href="/login" class="btn btn-warning">login</a>
                            {{/if}}
                        </div>

            </header>
            <main class="container">
                <div class="row">
                <nav class="col-3">
                    <div class="nav flex-column nav-pills">
                            <a class="nav-link active" id="v-pills-home-tab" data-toggle="pill"  href="/" role="tab" aria-controls="v-pills-home" aria-selected="true">Home</a>
                            <a class="nav-link" href="/profile" role="tab" aria-controls="v-pills-profile" aria-selected="false">Profile</a>
                            <a class="nav-link" href="/leaderboard">Leaderboard</a>
                            <a class="nav-link" href="/vote">Vote</a>

                    </div>
                </nav>
            <section class="col-7">
                {{{ body }}}
            </section>
            </div>
            </main>
            <footer>
            Thanks for visiting!
            </footer>
        </div>
        {{#if loggedIn}}
        <script src="/js/logout.js"></script>
        {{/if}}
    </body>

</html>